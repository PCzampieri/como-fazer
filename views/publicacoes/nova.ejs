<%- include('../header') %>

<br/>
<div class="container">
    <form method="POST" action="/publicacoes/nova">
        <div class="form-group">
                <label for="titulo">Título</label>
                <input type="text" class="form-control" id="titulo" name="titulo" placeholder="digite o título"/>        
        </div>
        <div class="form-group">
                <label for="categoria">Categoria</label>
                <select class="form-control" id="categoria" name="categoria">
                        <% categorias.forEach( categoria => { %>
                        <option value="<%= categoria.id %>"><%= categoria.categoria %></option>
                        <% }) %>
                </select>                  
        </div>         
        <div class="form-group">
                <label for="conteudo">Conteúdo</label>
                <textarea class="form-control" id="conteudo" name="conteudo" placeholder="digite o conteúdo"></textarea>        
        </div>  
        <button type="submit" class="btn btn-info">Salvar</button>
    </form>       
</div>
<%- include('../footer') %>


